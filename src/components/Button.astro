---

const { text, icon } = Astro.props;

---

<button class="bg-white text-black font-bold py-4 px-4  flex items-center justify-center space-x-2 text-center">
    {text}
    {icon && <i class={` ${icon} ml-2`}></i>}
</button>
