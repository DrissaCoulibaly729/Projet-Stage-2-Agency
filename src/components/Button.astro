---
// src/components/Button.astro
const { text, icon } = Astro.props;

---

<button class="bg-white hover:bg-green-800 text-black font-bold py-2 px-4 rounded flex items-center justify-center space-x-2 text-center">
    {text}
    {icon && <i class={` ${icon} ml-2`}></i>}
</button>
